CREATE DATABASE GymOrganization;
USE GymOrganization;


CREATE TABLE ACTIVIDAD(
	ID_ACTIVIDAD INT PRIMARY KEY AUTO_INCREMENT,
	NOMBRE VARCHAR(50) NOT NULL,
	CUPO_MAXIMO INT NOT NULL,
	PRECIO DECIMAL(10,2) NOT NULL
	);
	

CREATE TABLE PAGO(
	ID_PAGO INT PRIMARY KEY AUTO_INCREMENT,
	FECHA_DE_PAGO DATE NOT NULL,
	ESTADO BOOL NOT NULL,
	MONTO_A_PAGAR DECIMAL(10,2) NOT NULL,
	ID_ACTIVIDAD INT NOT NULL,
	FOREIGN KEY (ID_ACTIVIDAD) REFERENCES ACTIVIDAD (ID_ACTIVIDAD)
);


CREATE TABLE CLIENTE(
	ID_CLIENTE INT PRIMARY KEY AUTO_INCREMENT,
	NOMBRE VARCHAR(50) NOT NULL,
	APELLIDO VARCHAR(50) NOT NULL,
	DNI VARCHAR(15) NOT NULL,
	TELEFONO VARCHAR(20) NOT NULL,
);
	
CREATE TABLE ASISTENCIA(
	ID_ASISTENCIA INT PRIMARY KEY AUTO_INCREMENT,
	FECHA DATE NOT NULL,
	PRESENTE BOOL NOT NULL
);

CREATE TABLE ACTIVIDAD_CLIENTE(
	ID_ACTIVIDAD INT NOT NULL,
	ID_CLIENTE INT NOT NULL,
	FECHA_INSCRIPCION DATE NOT NULL,
	ID_PAGO INT NOT NULL,
	PUEDE_ESTAR BOOL NOT NULL,
	FOREIGN KEY (ID_ACTIVIDAD) REFERENCES ACTIVIDAD(ID_ACTIVIDAD),
	FOREIGN KEY (ID_CLIENTE) REFERENCES CLIENTE(ID_CLIENTE),
	FOREIGN KEY (ID_PAGO) REFERENCES PAGO(ID_PAGO)
);


Comentarios:::

Bueno ahi cree las tablas con clientes,actividad,pago y la relacion entre cliente y actividad y esa relacion se conecta con pago
Igualmente luego te lo explicare cuando hagamos llamada, faltan cosas y conexiones pero lo basico esta y seguro en el proceso
tengamos que cambiar logica pero bueno para que lo veas y ante cualquier pregunta me digas y te respondo.